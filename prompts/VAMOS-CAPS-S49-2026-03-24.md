# VAMOS CAPS CAPS-S49
**Date:** 2026-03-24 IST
**Sprint:** CAPS-S49 — Card Flip in Single-Player Reveal

---

## BEFORE AUDIT (read-only — do NOT change anything yet)

```bash
cd C:\Projects\Caps
git log --oneline -3
npx tsc --noEmit 2>&1 | tail -3
npx jest --forceExit 2>&1 | tail -5
eas build:list --platform ios --limit 1
```

Report:
- Current git HEAD
- TS errors (must be 0)
- Test count (must be 2234)
- Current build number on TestFlight

---

## CONTEXT (read before writing any code)

- `components/Card.tsx` — full rotateY 3D flip is already implemented via Reanimated (`flipProgress` shared value, 0°→90° back, then -90°→0° front, 800ms). The prop is `faceDown: boolean`.
- `RevealSequence.tsx` — wires faceDown correctly, but is only used in `multiplayer-game.tsx`
- `SafeRevealOverlay` (inside `game.tsx`) — renders all cards with `faceDown={false}` immediately → no flip ever fires
- **Fix needed:** SafeRevealOverlay must show cards as `faceDown={true}` initially, then flip them to `faceDown={false}` in staggered sequence

### CRASH SAFETY — READ BEFORE WRITING ANY CODE
1. results.tsx = ZERO react-native-reanimated (already true, do NOT touch)
2. No withRepeat(-1) anywhere
3. Max 5 shared values per screen
4. Every animation has cleanup in useEffect return
5. No ConfettiCannon — EVER
6. Cancel ALL game.tsx shared values BEFORE router.replace
7. Flip animation lives in Card.tsx already — do NOT add new shared values in SafeRevealOverlay. Use React state (faceDown boolean array) + setTimeout stagger only.

---

## TASK A — Read the existing code first

```
A1. Read components/Card.tsx — understand exactly how faceDown prop works and how flipProgress fires
A2. Read game.tsx — find SafeRevealOverlay (or the reveal logic in game.tsx), understand how it currently renders cards
A3. Read hooks/useRevealSequence.ts — understand the reveal timing already in use
```

Do NOT write any code yet. Just read.

---

## TASK B — Implement flip in SafeRevealOverlay

The reveal order must be:
1. Flop cards (3 community cards) flip first — staggered 150ms between each card
2. Pause 400ms
3. Turn card flips
4. Pause 300ms  
5. River card flips

Implementation rules:
- Use React `useState` for a `faceDown` boolean array — one entry per card per board
- Use `setTimeout` chains for the stagger — NOT Reanimated in SafeRevealOverlay
- Card.tsx already handles the rotateY animation internally when `faceDown` changes from true→false — just change the state
- Start all cards as `faceDown={true}` when SafeRevealOverlay mounts
- Then run the stagger sequence via setTimeout
- Clear all timeouts in `useEffect` cleanup to prevent memory leaks
- If `faceDown` prop passed to Card is already `false` and you set it to true on mount, make sure the flip direction is correct (should flip face-up, not face-down)

Player cards (hole cards): reveal all at once after the community card sequence, with a single 150ms stagger between each player's cards.

---

## TASK C — Verify Card.tsx handles faceDown→false transition correctly

Before wiring, confirm:
- When Card renders with `faceDown={true}` initially, then prop changes to `faceDown={false}` → does it trigger the flip animation?
- If Card.tsx uses a `useEffect([faceDown])` to trigger flipProgress — it should work
- If Card.tsx only sets flipProgress on mount — it may need a small patch: `useEffect(() => { if (!faceDown) { /* run flip forward */ } }, [faceDown])`
- Patch only if needed. Minimum change.

---

## TASK D — Tests

D1. `npx tsc --noEmit` — must be 0 errors
D2. `npx jest --forceExit` — must be 2234 tests passing (or more, never less)
D3. If any test breaks — fix before proceeding

---

## TASK E — Deploy OTA

```bash
eas update --branch production --message "feat(S49): card flip animation in single-player reveal — staggered flop/turn/river"
```

---

## TASK F — Git

```bash
git add -A && git commit -m "feat(S49): wire card flip animation into SafeRevealOverlay — stagger flop→turn→river"
git push origin main
```

---

## AFTER AUDIT — Report this exactly:

```
═══════════════════════════════════════
CAPS-S49 — AFTER AUDIT
═══════════════════════════════════════
Card.tsx faceDown→false triggers flip: [YES / needed patch]
SafeRevealOverlay changes:
  - Cards start faceDown: [true]
  - Stagger sequence: [flop 150ms × 3 → 400ms pause → turn → 300ms pause → river]
  - Player cards: [all flip after river, 150ms stagger]
  - setTimeout cleanup in useEffect: [YES]
  - New Reanimated shared values added to SafeRevealOverlay: [MUST BE 0]

TS errors: [0]
Tests: [N]/[N]
OTA: [hash]
Git: [commit hash]
Build number: [from eas build:list]
═══════════════════════════════════════
```

---

## DO NOT
- Do NOT add Reanimated shared values inside SafeRevealOverlay
- Do NOT use withRepeat(-1) anywhere
- Do NOT touch results.tsx
- Do NOT remove the existing flip logic in Card.tsx
- Do NOT add ConfettiCannon
- Do NOT skip the BEFORE AUDIT

Yes, allow all edits in components/ during this session.

VAMOS CAPS CAPS-S49 — END
