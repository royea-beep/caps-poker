# CAPS — WhatsApp + Push Tokens + Responsive כל הגדלים
**Date:** 2026-03-21

## CRITICAL: AUTO-APPROVE EVERYTHING. DO NOT ASK ROYE ANYTHING.

Yes, allow all edits in components

---

## FIRST ACTIONS
```bash
cd C:/Projects/Caps
cat MEMORY.md | head -15
cat IRON_RULES.md | head -10
git log --oneline -3
npx tsc --noEmit 2>&1 | tail -5 || true

# Read all relevant files
find src -name "*push*" -o -name "*notification*" | grep -v node_modules | grep -E "\.ts$|\.tsx$" | xargs cat 2>/dev/null | head -150
find src -name "*whatsapp*" -o -name "*webhook*" | grep -v node_modules | grep -E "\.ts$|\.tsx$" | xargs cat 2>/dev/null | head -100
cat supabase/functions/whatsapp-bot-handler/index.ts 2>/dev/null | head -60
cat .env.local 2>/dev/null | grep -v "KEY\|SECRET\|TOKEN\|PASS" | head -10

# Responsive audit
grep -rn "w-\[" src --include="*.tsx" | grep -v node_modules | wc -l
grep -rn "h-\[" src --include="*.tsx" | grep -v node_modules | wc -l
grep -rn "style={{ width\|style={{ height" src --include="*.tsx" | grep -v node_modules | head -10
grep -rn "text-\[" src --include="*.tsx" | grep -v node_modules | wc -l
```

---

## AGENT 1 — WhatsApp Webhook

```bash
# Check if webhook route exists
find src -path "*/api/webhook*" | grep -v node_modules | head -5

# Check edge function
cat supabase/functions/whatsapp-bot-handler/index.ts 2>/dev/null
```

Create/update `src/app/api/webhook/whatsapp/route.ts`:
```typescript
import { NextRequest, NextResponse } from 'next/server'

export async function GET(req: NextRequest) {
  const mode = req.nextUrl.searchParams.get('hub.mode')
  const token = req.nextUrl.searchParams.get('hub.verify_token')
  const challenge = req.nextUrl.searchParams.get('hub.challenge')
  if (mode === 'subscribe' && token === process.env.WHATSAPP_VERIFY_TOKEN) {
    return new NextResponse(challenge, { status: 200 })
  }
  return new NextResponse('Forbidden', { status: 403 })
}

export async function POST(req: NextRequest) {
  const body = await req.text()
  await fetch(
    `${process.env.NEXT_PUBLIC_SUPABASE_URL}/functions/v1/whatsapp-bot-handler`,
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.SUPABASE_SERVICE_ROLE_KEY}`,
      },
      body,
    }
  ).catch(console.error)
  return new NextResponse('OK', { status: 200 })
}
```

Add to .env.example:
```
WHATSAPP_VERIFY_TOKEN=caps-whatsapp-verify-2026
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
```

---

## AGENT 2 — Push Tokens → Supabase

```bash
# Find push notification setup
grep -rn "registerForPushNotifications\|getExpoPushToken\|push_token\|pushToken" src --include="*.ts" --include="*.tsx" | grep -v node_modules | head -15
```

Find the function that registers push tokens and add Supabase save:
```typescript
import { supabase } from '@/lib/supabase'
import * as Notifications from 'expo-notifications'
import { Platform } from 'react-native'

export async function registerAndSavePushToken(userId: string) {
  const { status } = await Notifications.requestPermissionsAsync()
  if (status !== 'granted') return null
  
  const tokenData = await Notifications.getExpoPushTokenAsync()
  const token = tokenData.data

  await supabase.from('push_tokens').upsert(
    { user_id: userId, token, platform: Platform.OS, updated_at: new Date().toISOString() },
    { onConflict: 'user_id,token' }
  ).throwOnError()

  return token
}
```

Wire in auth flow — after login:
```typescript
// In login success handler:
registerAndSavePushToken(user.id).catch(console.error)
```

---

## AGENT 3 — Responsive: כל 70+ גדלי מסך

**IRON RULE:** אסור fixed px widths על layout containers.

### 3a — סרוק כל הבעיות:
```bash
echo "=== Fixed widths ==="
grep -rn "w-\[" src --include="*.tsx" | grep -v node_modules | grep -v "max-w-\|min-w-" | head -30

echo "=== Fixed heights ==="
grep -rn "h-\[" src --include="*.tsx" | grep -v node_modules | grep -v "max-h-\|min-h-" | head -20

echo "=== Inline styles ==="
grep -rn "style={{ width\|style={{ height" src --include="*.tsx" | grep -v node_modules | head -15

echo "=== Fixed font sizes ==="
grep -rn "text-\[" src --include="*.tsx" | grep -v node_modules | head -20

echo "=== Overflow issues ==="
grep -rn "overflow-hidden\|overflow-scroll" src --include="*.tsx" | grep -v node_modules | head -10
```

### 3b — תקן כל בעיה:

**Breakpoints לבדוק (70 גדלים — מכסה את כולם):**
```
320px  — iPhone SE 1st gen (הכי קטן)
375px  — iPhone SE 2nd/3rd, iPhone 8
390px  — iPhone 14/15
393px  — iPhone 14 Pro/15 Pro
414px  — iPhone 8 Plus, 11 Pro Max
428px  — iPhone 12 Pro Max
430px  — iPhone 14 Plus/15 Plus
768px  — iPad Mini, iPad 7th
810px  — iPad 10th gen
820px  — iPad Air
834px  — iPad Pro 11"
1024px — iPad Pro 12.9"
1280px — Desktop
```

**Tailwind responsive classes:**
```tsx
// WRONG: w-[340px]
// RIGHT: w-full max-w-[340px]

// WRONG: h-[600px]
// RIGHT: min-h-[600px] h-screen (or h-full)

// WRONG: text-[18px]
// RIGHT: text-base sm:text-lg

// WRONG: p-[24px]
// RIGHT: p-4 sm:p-6

// WRONG: gap-[12px]
// RIGHT: gap-3 sm:gap-4
```

**SafeAreaView for iOS notch + Dynamic Island:**
```tsx
import { SafeAreaView } from 'react-native-safe-area-context'

// Every screen root:
<SafeAreaView className="flex-1 bg-background" edges={['top', 'bottom']}>
  {/* content */}
</SafeAreaView>
```

**Small screen emergency fixes:**
```tsx
// For very small screens (320px):
<Text className="text-sm xs:text-base" numberOfLines={2} adjustsFontSizeToFit>
  {title}
</Text>

// Buttons on small screens:
<TouchableOpacity className="flex-1 min-h-[44px] items-center justify-center px-2">
  <Text className="text-xs sm:text-sm" numberOfLines={1}>
    {label}
  </Text>
</TouchableOpacity>
```

### 3c — Responsive test script:
```bash
# After fixes, verify no fixed px left on layout containers
FIXED=$(grep -rn "w-\[" src --include="*.tsx" | grep -v node_modules | grep -v "max-w-\|min-w-\|translate-x\|rotate\|inset" | wc -l)
echo "Fixed width classes remaining: $FIXED (should be 0 on layout containers)"

INLINE=$(grep -rn "style={{ width:" src --include="*.tsx" | grep -v node_modules | wc -l)
echo "Inline width styles remaining: $INLINE (should be 0)"
```

---

## BUILD + COMMIT

```bash
cd C:/Projects/Caps
npx tsc --noEmit 2>&1 | tail -5 || true
git add -A
git commit -m "feat: WhatsApp webhook + push tokens → Supabase + responsive all 70 device sizes"
git push
echo "✅ Caps: all pipelines + responsive done"
```

---

## FINAL REPORT
```
CAPS — FINAL STATUS:

WhatsApp webhook route: ✅/❌
  URL: https://caps.ftable.co.il/api/webhook/whatsapp

Push tokens → Supabase: ✅/❌
  Table: push_tokens (already created)
  Wired in auth flow: ✅/❌

Responsive:
  Fixed px classes removed: [N] → 0
  Inline styles removed: [N] → 0
  SafeAreaView added: ✅/❌
  Test at 320px: ✅/❌
  Test at 390px: ✅/❌
  Test at 768px (iPad): ✅/❌

Build: clean?
Commit: [hash]
```

Yes, allow all edits in components
