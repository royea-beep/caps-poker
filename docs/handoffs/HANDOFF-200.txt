VAMOS CAPS DELETE-THE-LIAR — handoff 200 — 2026-09-08
Copy 2 of 3. Copy 1 is vamos_handoffs row 200 (slug delete-the-liar-2026-09-08).
Copy 3 was the PASTE block in the session output.
Full detail: docs/infra/DELETE-THE-LIAR-2026-09-08.md

Branch claude/vamos-caps-align-celebration-flppo0, commit 495d07a. NOT merged, no bump.
Full suite 2,866/2,866 across 55 suites. tsc clean. No app code touched. No build dispatched.
No Edge Function deployed. No dollar figure claimed.

=== §1 THE DELETION ===

WHICH WORKFLOW ACTUALLY BUILT 515 — .github/workflows/ios-testflight.yml
Checked from the run record and the build INPUT, not inferred from any name:
  run 1227 (id 34107249013), workflow_dispatch on main, conclusion SUCCESS
  2026-09-07 09:39:25Z -> 09:53:29Z, head b8b7ae1a3
And not from the commit message either. That workflow reads its build number with
`node -p "require('./app.json').expo.ios.buildNumber"` (its own step, line 72), and
`git show b8b7ae1a3:app.json` reads ios.buildNumber 515, version 2.7.0.
Same workflow succeeded for 509, 510, 511, 512, 513, 514 and 515 — seven consecutive dispatches.

NOTHING REFERENCED THE DISABLED ONE — checked three ways:
  · no `workflow_call` anywhere in .github/workflows/, no `uses: ./` local reference
  · no script, no config, no doc INSTRUCTING a dispatch
  · nothing dispatches it by its GitHub display name either
The seven repo-wide hits are all prose: MEMORY.md:951, docs/BUILD-508-2026-08-28.md:91,
CLAUDE.md:206, docs/handoffs/HANDOFF-199.txt:84, docs/infra/CLOSE-THE-THREE:160, and two lines
in ios-testflight.yml's own header.

⚠️ AND IT HAD NEVER RUN. total_count 0 — zero runs in its entire existence — while GitHub listed
it as state "active" with a live workflow_dispatch trigger, importing a p12 from
BUILD_CERT_P12_BASE64 and signing with cert 45EBC138DF94E77658BA9558EAAE19FC, which its own
header calls revoked. Apple hard-limits roughly ten uploads per app per day.

AND ITS STATED KEEP-CONDITION WAS MET. ios-testflight.yml said, in its header:
  "ios-testflight-DISABLED.yml is deliberately KEPT until this one produces a successful build,
   so there is a diff to compare against when something breaks."
Satisfied seven times over.

DELETED. And the two header lines in ios-testflight.yml that named it were rewritten in the SAME
commit — a comment pointing at a file that no longer exists is exactly the defect being closed.
The header now records which builds that workflow produced, how that was checked, and where the
diff still lives:  git show 9577aa5:.github/workflows/ios-testflight-DISABLED.yml
YAML re-parsed after the edit: one job, workflow_dispatch trigger, name unchanged.

=== §2 SIBLING SWEEP — NAMES THAT LIE ABOUT THEIR STATE ===
Nothing below was changed. Roye decides.

⚠️ asc-details.yml ("ASC Build Details", id 276765913) and asc-list.yml ("ASC List Builds",
   id 256838216) are both listed by GitHub as state "active" — and NEITHER FILE EXISTS in the
   working tree, on origin/main, or as a single object anywhere in this clone's history. Two
   workflows in the Actions UI backed by nothing. The listing asserts a state the repo does not
   have — the same class inverted.
⚠️ ios-testflight-free.yml is named "iOS TestFlight (FREE — macOS runner, no EAS cloud)" and its
   line 92 runs `eas build`, against the Expo account ios-testflight.yml says is disabled and not
   coming back. Last run: run 59, 2026-06-25. It is also what the DELETED file's header called its
   replacement — already untrue, since the real replacement is ios-testflight.yml.
⚠️ ios-simulator-smoke.yml runs `eas build` (line 82) on a PUSH trigger. Same dead account.
⚠️ variant="gold" survives at app/simulate.tsx:232 and :323. components/Button.tsx:192 calls that
   variant "the de-facto primary CTA" and it has painted MINT since the theme sweep. The route
   redirects to Home so nobody sees it — the string is still a name asserting a colour.
⚠️ MEMORY.md:951 instructs a future session to wire a new certificate into "the restored
   ios-testflight-DISABLED.yml" — a doc pointing at the file just deleted. NOT edited: MEMORY.md
   is outside this session's allowed paths. It needs one line changed to name ios-testflight.yml.

✅ NOT LIARS, checked and withdrawn:
   KILL_Board / KILL_game — KILL_FINITE_ON_THIS_PLATFORM = true, so the name matches the state.
   The animationKill.ts:43 line reading "With KILL_Board false on web" is honest HISTORY of a
   shipped experiment, and line 62 records the revert. Not a stale claim.
   auto-fix-crashes.yml and claude-fix.yml carry schedule / repository_dispatch triggers but are
   disabled_manually at the GitHub level — the mismatch runs in the SAFE direction.

=== §3 SUCCESSFUL BUILDS PER DAY — RE-MEASURED TODAY ===
Counts only. No dollar figure; this session has no billing access.
Each successful run is a real container: expo export + Playwright + WCAG + BackstopJS + prod.
Failed and cancelled runs allocate nothing.

  day        success  failure  cancelled  total
  2026-08-22    16       0         4        20
  2026-08-23    13       7         3        23
  2026-08-27    14       1         7        22
  2026-08-28     2       0         2         4
  2026-08-30     3       0         1         4
  2026-08-31     3       0         0         3
  2026-09-01     6       0         1         7
  2026-09-02     4       0         1         5
  2026-09-05     1       0         0         1
  2026-09-07     7       0         3        10
  2026-09-08     1       0         0         1

⚠️ THEY DID NOT DROP. AND NOTHING UNKNOWN IS TRIGGERING BUILDS — the cause is known:
  · git show origin/main:.github/workflows/web-deploy.yml still reads `branches: [main, master]`
    with NO paths-ignore at all. Re-checked today after a fresh fetch.
  · ec17b41, which added it, is still NOT an ancestor of origin/main (still 8606d64).
  · The newest Web Deploy run of any kind is still 2026-09-08 07:39:12Z, on main — the same one as
    yesterday. The rule was committed at 08:25:34Z, FORTY-SIX MINUTES LATER, and no run has
    started since. There have simply been no pushes to main.
The 7 -> 1 between 09-07 and 09-08 is a quiet day, not a saving.

WHAT IT WOULD SAVE, recomputed from the same 100 runs: of 70 successful runs, 24 (34%) had a
head-commit diff entirely of docs/** or **/*.md. Aimed correctly. Saved nothing yet.

⚠️ THE CHECK THAT SETTLES IT: after the next merge to main, a docs-only push must show NO Web
Deploy run. Until that is observed the rule is untested in production.

=== FILES ===
  .github/workflows/ios-testflight-DISABLED.yml   DELETED
  .github/workflows/ios-testflight.yml            header corrected — it named the deleted file
  CLAUDE.md                                       entry replaced
  docs/infra/DELETE-THE-LIAR-2026-09-08.md        NEW — the full record

=== FOR ROYE ===
1. The liar is gone. The build path is unaffected: ios-testflight.yml built 509 through 515 and is
   the only workflow that builds CAPS.
2. Four more names still lie, none deleted: two ghost workflows GitHub lists with no file, a "FREE"
   workflow that calls a dead Expo account, a smoke test that does the same on push, and a gold
   button that paints mint. Say the word on any of them.
3. The billing fix STILL is not live. It needs a merge to main.
