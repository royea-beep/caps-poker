# FINAL-QA-512 — 2026-09-01 — the tester candidate

Build 512 is the one that goes to testers. Everything the bot can do alone is done: security closed
and attacked, economy defined and reset to a clean zero-gap baseline, two currencies separated, nav
cut to three tabs, D1 home, working shop, 511 on TestFlight. This is the last gate before real people.
**Honest about the instrument's reach throughout.**

## Self-test — the instrument caught its plants
Before trusting any render, the QA instrument planted a deliberate **horizontal overflow** (a 3000px
element) and a deliberate **clip** (an `overflow:hidden` 40px box cutting a long child) and asserted
its own detectors fired on **both**. Result: `overflow_caught=true clip_caught=true`. The instrument
is not blind, so its results below are trustworthy. (This is the rule that has caught four tools in
this series, one a canary that failed to fail.)

## Loop coverage + boundary
| Check | Result | How |
|---|---|---|
| Test suite | **41 suites / 2,649 tests pass** | `jest` (exit 0) |
| Typecheck | **exit 0, clean** | `tsc --noEmit` |
| Production web build | **builds; deployed & promoted** | web-deploy.yml → Vercel |
| DB state (Rule 9) | reset held, gap 0 | fresh SELECT |
| **Mirrored-bytes render** | **mounts at 320/375/393/430** | deployed bundle served locally |
| Rules | tie / boards-decide / one-deck green | `verify-rules.test.ts` in the suite |

**Boundary — what was rendered vs code-read vs untestable here.** The live browser loop still cannot
run against the live domain (the agent proxy resets Chromium's connection) and my *local* `expo export`
bundle uses a classic `<script defer>` that trips the documented `import.meta` bootstrap error. **What
DID work this sprint: a mirrored-bytes render** — I fetched the *deployed* HTML+bundle (whose script tag
is `type="module"`, so `import.meta` is valid) and served the bytes from localhost, and **the app
mounted.** So the nav/layout below is **rendered from the shipped bytes**, not merely code-read — but it
is **not** the live site and **not** a device. Native font rendering (the Georgia masthead), iOS Dynamic
Type, real multiplayer load, and two physical devices remain **untestable here**.

## Economy on real hands — real moves, practice doesn't, reset held
- **Reset held (fresh SELECT):** 489 devices **all at 2,000**; **float 978,000 = ledger 978,000, gap 0**;
  bindings 6, trophies 6, achievements 154, hand_history 71 intact; archive `chip_transactions_prereset_
  20260901` present; `starting_chips=2000`.
- **A stale device adopts 2,000 on mount:** the mirrored render (a fresh browser, no local balance)
  showed **💰 2,000** on home and the shop — the server-wins bootstrap adopting the reset baseline.
- **Real moves, practice doesn't** (proven last sprint against the untouched live `record_hand_net`):
  a real hand wrote `hand_net +150` and moved the balance; a practice hand wrote `hand_net 0` and did
  not. `record_hand_net`/zero-sum untouched this sprint.

## Rules — once more
`verify-rules.test.ts` (green in the 2,649): a 3-player one-board-each hand derives as a **tie** for
every seat; **boards decide the win** with the right board/card/community counts at 2P/3P/4P; **one
deck**; `record_hand_net` is the **sole per-hand balance mover** (one writer, read-back write).

## Nav — rendered from the shipped bytes
At 375 and 393 the mirrored render shows a **three-tab bar: 🏠 Home · ♠️ Play · 👤 Profile** (labels
hidden at ≤375, the boundary). **Cups and Friends are not in the bar.** Reachability, all rendered:
- `/cups` → the **CUPS** screen ("CUPS 0/0 cups"), with the 3-tab bar beneath — Cups lives in Profile.
- `/friends` → **CLUBS** ("Your circle · play only your friends · Create · Join") — Friends in the side menu.
- `/chip-store` → **redirects to `/shop`** (rendered: "🎰 Chip Shop 💰 2,000").
- `/heatmap` → **redirects to `/`** (home). No dead route a URL can still hit.
- Side menu (rendered): 👥 Friends · Battle Pass · Coaching · Tutorial · Language — no duplicate destinations.

## Console — clean
**Zero console errors** across all four widths and every route in the mirrored render. **Zero horizontal
overflow** at 320/375/393/430. No pre-existing errors surfaced.

## Web deploy — verified by CONTENT DELTA, not hash
Live bundle `index-15217bda56fcc8f0a75a69e33b1f5240.js` carries commit **b79541a** (old cb4c37f absent).
Two-currency labelling live: **"Virtual chips only" (1) · "No real-money gambling" (2) · "Practice vs
bots" (3)**; the reset shows as the rendered **2,000** balance.

## iOS build 512
- Dispatched `ios-testflight.yml` on `main` @ 512 — run **33469592848**, commit `b79541a`.
- **Delivery:** _in flight at this commit; the altool `UPLOAD SUCCEEDED` line + Delivery UUID + byte
  size are quoted verbatim in the follow-up commit and handoff. A green workflow is not a delivered
  build; confirmed by reading the log, not the check._
- **DB build keys still absent** — `app_config` has no `current_build`/`next_build_number` (0). Only
  `ios.buildNumber` moved 511→512; `extra.buildNumber` (330) abandoned.

## Baselines — known-stale, not regenerated (deliberate)
The BackstopJS references (`backstop_data/bitmaps_reference/`) are **known-stale**: the home changed with
D1 and the nav went 5 tabs → 3 since they were captured. They are **not regenerated** this sprint —
three baseline sets in this series were wrong and only *looking before committing* caught them, and a
non-blocking visual gate is not worth baking a regression into on a tester-candidate build. Regenerate
via `backstop-baseline.yml` on Linux and review the diff before committing, in a dedicated pass.

## THE DEVICE-ONLY TAPS — listed, not claimed (Roye's taps on 512)
⟦IRREDUCIBLE⟧ = no web pass, mirrored or live, can ever replace it.
1. ⟦IRREDUCIBLE⟧ **The D1 masthead font** — "CAPS" renders in **Georgia** on device (Playfair is not in
   the binary; the native serif stack substitutes). The mirror shows the masthead in Chromium's serif,
   **not** the iOS font — only the binary shows the real face. Also the **solid gold** (not a gradient)
   and the **C1 icon at 60px**.
2. **The three-tab bar at `<375`** where labels hide (iPhone SE, or Display Zoom on). The mirror rendered
   this at 375 with the labels hidden and no crowding — but the physical SE at true device DPI is the
   real check.
3. ⟦IRREDUCIBLE⟧ **The multiplayer label at iOS largest text size** (Dynamic Type) — an OS-level scale
   the web does not reproduce. Plus the **lobby practice row**, **REMATCH** as a whole flow, and a
   **tied-board hand** reading the tally on device.

## THE HONEST FINAL WORD
- **Is 512 a tester build? Yes.** The suite is green, types clean, the web build deploys and the shipped
  bytes render at all four widths with a correct three-tab nav, working redirects, no console errors and
  no overflow; the economy is on a clean zero-gap baseline that a stale device correctly adopts; the
  security holes are closed and were re-attacked. **What I would not want a tester to hit:** nothing
  blocking is known. The one honest soft spot is anything that depends on **native rendering** (the
  Georgia masthead has genuinely never been seen) and **live multiplayer under real concurrency** —
  neither is broken as far as anything here can tell, but neither has been seen for real.
- **What has genuinely never been verified, stated once:** native on-device rendering (fonts, Dynamic
  Type, true-DPI layout), multiplayer under real load, and two physical devices playing a real hand
  against each other. Invite testers knowing that is the exact edge of what is proven.
- **Weighting:** across this series more filed defects were measurement error than real. I did not
  invent a blocker; I am not hiding one. 512 is a sound tester candidate.

## Production unchanged
No economy value, faucet, rake, reset, security fix, art, nav-beyond-the-shipped-3-tabs, or flag touched
this sprint; `KILL_Board`/`KILL_game` untouched; payments off; nothing to App Store Connect. The only
change is the 512 build-number bump on top of the already-merged reset.
