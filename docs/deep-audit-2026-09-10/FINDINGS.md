# Findings — ranked by cost to the product, measured 2026-09-10 on `ef55640` + live DB

Every finding below was verified on two sources by the orchestrator (a query and a file, or a file and its consumer). Agent-sourced findings were re-opened at the cited line before being listed. "Fixed" means changed on branch `claude/caps-deep-audit-nr8z17`; "patch" means in `root-docs.patch`; "reported" means off-limits or not provably safe from here, with the fix written out.

| # | cost | class | finding | status |
|---|---|---|---|---|
| 1 | HIGH | security · name≠content | **The pre-reset ledger archive is readable and writable with the anon key.** `chip_transactions_prereset_20260901` has RLS OFF and every grant to `anon`; `SET ROLE anon; SELECT count(*)` → 4,237 rows of device_id, user_id, amount, description. The live ledger is service-role-only; its copy is not. No function, view, cron or repo file reads it. | reported — SQL below |
| 2 | HIGH | security · policy governs nothing | **Seven SECURITY-DEFINER views hand anon what the tables deny.** Measured as anon: `v_grant_without_session` 7 rows of device_id + total_chips; `v_harness_devices_v2` 8 device_ids — a UNION over `v_harness_devices`, which migration 20260831170000 explicitly REVOKED from anon ("should not be a public listing of device ids"), and `v_simulator_devices`; `friction_heatmap`/`top_*` read `analytics_events`, whose comment says READS ARE service_role ONLY. Migration 20260907000000 created `_v2` without the revoke. device_id is the identity every economy RPC keys on. | reported — SQL below |
| 3 | HIGH | repo ≠ production | **main is 33 commits behind the branch that runs.** `claude/vamos-caps-align-celebration-flppo0` carries `20260908000000_close_submit_score_chip_faucet.sql` (live in the ledger as `20260908213547`), 14 test files, the corrected CLAUDE.md and 9 docs. Every bot that starts from main's CLAUDE.md read "submit_score STILL OPEN" for two days after it closed. | CLAUDE.md pointer fixed; merge is Roye's |
| 3a | HIGH (ops) | what is written is not what runs | **Two edge functions run code the repo has never held.** `whatsapp-bot-handler` (deployed v70, 2026-07-15; repo last touched 2026-05-17) and `crash-analyzer` (v17, 2026-07-15) send outbound WhatsApp through Empire HQ `empire-messaging`; the repo copies POST straight to Twilio, `!`-assert `TWILIO_WHATSAPP_FROM` at module load, and the repo `whatsapp-bot-handler` sends a real "Direct test" message to a hardcoded personal number in its diagnostic branch. A deploy from the repo reverts the egress route and refuses every send if that secret is absent — the `seo-prerender` shape. Deployed bytes saved and diffed; `resolve-hand` (the settlement writer) and the other seven repo-backed functions are byte-identical. | reported — commit the deployed source before anyone deploys; `EDGE-FUNCTIONS.md` |
| 4 | HIGH (when testers hold balances) | control that cannot fire · number never asserted | **Nothing computes the float-vs-ledger gap.** `health_check()` sums `leaderboard.total_chips` and counts ledger rows but never compares them; `run_daily_reconciliation` reconciles prompt-log counts with Empire HQ; the tripwires read neither. "Gap 0" is a hand measurement every sprint, and handoff 202 records that today's 0 rests on a one-off 2026-09-01 backfill — the signup 2,000 is not ledgered at grant time. | reported — needs a tripwire on the economy path |
| 4a | HIGH (CI integrity) | guard that cannot fail | **The blocking WCAG gate passes when the site cannot be loaded.** `tests/wcag-audit.js:68-73`: a route whose `goto` throws gets `routeEntry.error` set and counts of 0, and the verdict at :84 reads only `totals.critical > 0`. `web-deploy.yml:207` runs it as "fail on critical" after a bare `sleep 45` (:197, a step *named* "Wait for caps.ftable.co.il to serve new SHA" that checks nothing). If alias propagation is slow or the storageState is broken, all 14 routes error, the gate prints PASS, and the audit certifies the previous build. | reported — `tests/` is in the allowance, but the fix is a design choice (fail on any errored route, or on `error` count > N) |
| 4b | HIGH | number printed, never asserted · name≠content | **The live payment-forge probe cannot fail.** `tests/verify-payment-lock.mjs:88-91` prints `expected : 401` beside `http : <status>` for every probe and never compares them; there is no exit code. Its header (:9-11) says `purchases` / `chip_transactions` "are counted before and after so a silent write would be visible" — no such count exists in the file. If a probe started minting, the run would print the 200 next to "expected 401" and exit 0. | reported — needs a real comparison and the counts the header promises |
| 5 | MEDIUM | name≠content — **the ninth** | **`assets/sounds/boardLose.wav` is byte-for-byte `boardWin.wav`** (sha256 `3c395893a27a6618…`, 44,178 B); so is `revealStart.wav`. Committed 2026-03-25 as placeholders (`b6e99ff` "revealStart sound placeholder") and never replaced. `components/BoardReveal.tsx:552/555` plays `boardWin` on a won board and `boardLose` on a lost one — the same chime. Sound is on by default (`utils/sounds.ts:165`). Never heard in QA: native has never been verified and the web rigs run muted. Three declared `SoundName`s (`turnReveal`, `riverReveal`, `boardTransition`) have no file at all. | reported — `assets/` is outside the edit allowance and needs real audio, not a rename |
| 6 | MEDIUM | name≠content | **The COMPLETE banner says "+50% BONUS" on every hand.** `utils/i18n.ts:977` (`'+50% BONUS'`, Hebrew :556) is a constant, while the bonus is `complete_bonus_pct_by_boards` {2: 25, 3: 50, 4: 75} via `utils/completeBonusPct.ts:65` — right for 3 players, wrong for 2P (4 boards, 75%) and 4P (2 boards, 25%). The chip amount beside it is correct. | reported — results-screen copy, economy-adjacent |
| 7 | MEDIUM | guard that cannot fire | **Settings "2 / 4 colour suits" paints nothing on in-game cards.** `components/Card.tsx:506` computes `suitColor` from `fourColorSuits`, then every V2 render at :613/:614/:621/:622/:630 uses `v2SuitColor = isRed ? V2_RED : V2_BLACK`. Only `StaticCard.tsx:54` (results) honours the toggle. | reported — card rendering |
| 8 | MEDIUM | name≠content | **The CLASSIC swatch in the theme picker is brown/gold; the theme paints green felt.** `app/settings.tsx:733` `bg: '#1a0800', accent: '#c9a84c'` beside a FIVE-O row that was corrected on 2026-09-03 (`#1A1A2E`/`#4FD6A8`). Same class as the maroon-felt line. | reported — one-line fix, Roye's colour call |
| 9 | MEDIUM | two sources of truth · control that cannot fire | **The dirty-shutdown detector is dead by construction.** `app/game.tsx:39` marks "active" through `@caps/debugger` (key `@caps_debugger/<app>_active`); `app/results.tsx:299` clears and `app/_layout.tsx:384` reads `utils/dirtyShutdown` (key `caps_game_active`). The key that is written is never read or cleared; the key that is read is never written. A mid-hand kill produces no WhatsApp alert. | reported |
| 10 | MEDIUM | number printed, never asserted | **15 probe scripts under `tests/` cannot fail.** `align-celebration-proof`, `delete-account-proof`, `elo-tie-proof`, `one-counter-proof`, `stats-classc-proof`, `dedupe-verify`, `felt-verify`, `live-bug-report-verify`, `live-unmix-verify`, `locale-he-verify`, `placement-verify`, `survey-verify`, `verify-payment-lock` have no `process.exit`, `exitCode`, `assert` or `throw`; `clubs-verify` and `lobby-v2-public-verify` have one `throw` each. They print and exit 0. Not in CI (Jest runs only `*.test.ts`), so the cost is a PASS line a human trusts. | reported — each needs its own assertion, not a blanket exit code |
| 10a | MEDIUM | guard that cannot fail | **`crash-analyzer` reports success on failed calls, twice.** `supabase/functions/crash-analyzer/index.ts:163-179`: the Anthropic call has no `res.ok` check, so a 4xx/5xx/529 body (no `content`) becomes cause `unknown` / confidence LOW and the row is stamped `analyzed` or `fixing` (:311) — the same defect fixed in `analyze-bug-report` on 2026-09-06, still live in the sibling (deployed v17, 2026-07-15). `:80` POSTs the GitHub `dispatches` call without reading the response, then :334 logs "triggered" and sends Roye "🔧 FIX TRIGGERED — GitHub Actions running — OTA in ~2 min" on a 401/404/422. | reported — edge function; diff deployed vs repo first (`EDGE-FUNCTIONS.md`) |
| 10b | MEDIUM | guard that cannot fail · number never asserted | **`tests/verify-full-loop.mjs` says ABORT and continues.** :210 prints `ABORT — the <engine> instrument cannot detect…` when its own self-test misses a planted defect, then proceeds (no exit, no throw) while the header (:19) and `docs/VERIFY-EVERYTHING-2026-08-31.md:35` promise it aborts. :227-234 then print seven defect counts ("navigation failed N / 264" …) and exit 0 whatever they are. | reported |
| 10c | MEDIUM | guard that cannot fail | **`tests/verify-ledger-writers.mjs` passes when every writer is dead.** :56-62: a writer counts as "✓ ran" unless the body matches an RLS-denial pattern or status is 403; `process.exit(blocked === 0 ? 0 : 1)`. A revoked key (401), a dropped function (404) or a wrong URL → 10 × "✓ ran", exit 0. | reported |
| 10d | MEDIUM | number never asserted | **`testflight-manage.yml` `answer-export-compliance` prints `PATCH -> HTTP <code>` and never checks it** (:387-390; the `abort` used at :383 shows the author knew how). A 401/409/422 from App Store Connect prints `now: nil`, the run is green, the build stays in Missing Compliance. The workflow comment calls this "the one blocker that is fixable from here". | reported |
| 10e | MEDIUM | number never asserted | **Five more verifiers compute a verdict and only print it**: `tests/verify-board-counts.mjs:157` ("N/12 cells render the board count" — the 2P=4/3P=3/4P=2 rule has no failing check), `tests/verify-gates.mjs:125` ("OPEN — buy surface is present" for `/chip-store`, `/simulate`, `/debug`, `/spectate`), `tests/evaluator-runtime-check.mjs:24` (`flushBeatsTrips`, `usesTwoAndThree` for the evaluator `resolve-hand` settles money with), `tests/economy-crosscheck.mjs:75` ("stored X vs rendered Y -> DISAGREE"), `tools/verify-cosmetics.mjs:43` (cue widths vs the settled 3/2/1 — settled area, the missing assertion is the finding). The only `process.exit` in the first four is a harness abort. | reported |
| 10f | LOW | number never asserted | `ios-simulator-smoke.yml:166-177,250-251` runs under `set +e`, prints each screenshot's byte size and "Total screenshots: N"; 0 bytes or 0 files is green (only the crash grep fails). | reported |
| 10g | LOW | guard that cannot fail | `telegram-bot-handler/index.ts:133-134` replies "AUTO-FIX ON/OFF." without reading the `app_config` upsert `{ error }`; the flag it claims to have flipped is read at :197 and by `whatsapp-bot-handler:904/1131` to decide whether crashes auto-apply. | reported |
| 11 | MEDIUM | stale docs | **The product map was wrong on reachability.** 3 routes marked R have no tap path (`/theme-pick`, `/orientation-pick`, `/spectate`), `/simulate` renders in dev builds, `/debug` opens on `__DEV__` not on the dev-unlock gesture, and 8 "reached by" cells named a screen with no link. | fixed in place — `SCREENS.md` |
| 12 | MEDIUM | stale docs | **Every current-shaped doc the audit read was STALE — 0 of 73 came back CURRENT.** 17 root docs (README describes a "club management app"; PROJECT-INFO.json carries bundle id `com.royea.capspoker`, never this app's; CURRENT-STATE says 2.6.0 / build 457 from 2026-04-18; IRON_RULES.md holds none of the numbered Iron Rules briefs cite) and 15 docs/ files. | CLAUDE.md fixed; 15 docs/ files bannered or corrected; 17 root files in `root-docs.patch` |
| 13 | MEDIUM | control that cannot fire | **`replicate-vault-to-supabase.yml` has never succeeded.** Its only run (2026-09-01, the monthly schedule) failed after 9 s; next fire 2026-10-01. A "second system" for the signing vault that reads as a backup. | reported |
| 14 | LOW now / HIGH if flipped | policy governs nothing | **`iap_enabled` cannot take effect.** `loadIapEnabled()` is called only from `components/StarterOfferModal.tsx:90`, and `<StarterOfferModal` is mounted nowhere, so `isIapEnabled()` returns its default `false` whatever the row says; the file header promises "reversible WITHOUT a build". `web_payments_enabled` has no importer. `battle_pass_enabled`, `practice_mode_enabled`, `quick_poker_enabled`, `maintenance_mode` have 0 client reads. All are meant to be off today. | reported — flags are off-limits |
| 14g | MEDIUM (latent) | guard that cannot fail · policy governs nothing | **`run_marathon` has no working off-switch.** `app/_layout.tsx:561-574` (native only) reads `app_config.run_marathon`, and if `requested` is true, upserts `{requested:false}` and two seconds later pushes `/game?autoSim=true&autoSimCount=10`. `app_config`'s only write policy is `service_write` (service_role), so the client upsert is refused silently (inside `catch {}`), and once the WhatsApp bot writes `requested:true` (`whatsapp-bot-handler:874-876`, reply "4") every native device auto-runs a 10-hand simulation after every launch until someone resets the row with service_role. Measured today: the row does not exist, so nothing fires. | reported |
| 14h | MEDIUM | policy governs nothing | **23 of 42 `app_config` keys are read by nothing — not the client, not an edge function, not any live SQL body** (`pg_proc` searched 2026-09-10, table below). Among them the two incident controls `maintenance_mode` and `min_app_version`, `emergency_chips_enabled` (credited as the gate for `/gameover` in `docs/product-map/BATTLE-PASS-CLOSED-2026-09-07.md:95`), and `daily_reward_chips` (150) — `claim_daily_reward` does not read it, so the amount lives in two places. Flipping any of these changes nothing, and a reader of `app_config` believes it would. | reported — flags are off-limits; the table is the deliverable |
| 14i | MEDIUM | instrument measures itself | **Four payment harnesses open a gate the web build never consults.** `tests/final-qa.mjs:137`, `tests/gated-payment-surfaces.mjs:29`, `tests/chip-store-forced.mjs:48`, `tests/buy-tap.mjs:12` route-override the `app_config` response for `iap_enabled`/`web_payments_enabled` on https://caps.ftable.co.il, then report on "the paid surface". On web nothing fetches those keys (#14), `/chip-store` is a redirect, and `/shop`'s paid half is behind `Platform.OS !== 'web'` — the surface they report on cannot render. `tests/verify-gates.mjs:67` likewise credits the `/chip-store` closure to "iap_enabled=false AND web_payments_enabled=false" ("closed by DATA") when the platform check closes it; it would print PASS with both flags true. | reported |
| 14j | LOW | dead-looking, load-bearing — FLAG, DO NOT REMOVE | `utils/sounds.ts:174` says `ambient.mp3 not yet present — infrastructure ready`; the file exists (720,604 B) and plays at 0.15 in every game (`app/game.tsx:597`, `multiplayer-game.tsx:340`). `cleanup_expired_rooms` has EXECUTE revoked from every client role and looks dead from the app — cron job 32 is its only caller. Dynamic `require`s in `components/ErrorBoundary.tsx:49,56`, `utils/sounds.ts:105`, `app/debug.tsx:57` are load-bearing. Four of six `ECONOMY_FLAGS` (`freeRefillEnabled`, `walletSyncEnabled`, `adRewardEnabled`, `sit_n_go_enabled`) and five `utils/economy.ts` helpers have zero readers while the product map credits "client flags off" for two of them. | noted |
| 14a | MEDIUM (latent — 0 devices affected today) | present-and-empty · two sources | **The solo results screen draws an ELO badge from a field solo hands can no longer move.** `record_hand_result_d` returns `elo_delta = COALESCE(leaderboard.elo_last_delta, 0)` (migration 20260831150000:101, live); since CLOSE-S2 (20260903:41) the only writer of `elo_last_delta` runs for `service_role` only, so a solo `quick_poker` hand returns whatever the row held — and `app/results.tsx:715` renders it at :1502 as this hand's "▲ 20 ELO". Measured today: 0 of 403 rows carry a non-zero `elo_last_delta` (the 2026-09-01 reset zeroed it and no MP hand has ever settled), so nobody sees it yet. The first device whose MP hand settles will then see that MP delta on every solo result screen after it, wins, losses and ties alike, while `/rank` does not move. | reported — results screen + ladder ruling; the fix is to return 0 (or NULL) for non-service-role rows |
| 14b | LOW | present-and-empty | `app/(tabs)/index.tsx:614-615`: `get_sng_activity_feed` is read as `{ data }` with `error` never destructured, so an RPC failure renders "Play Sit and Go to see your history" (#15) to exactly the veteran who has history — one branch for two opposite conditions. | reported |
| 14c | LOW | binary over three-way | `components/Board.tsx:1003` passes `isWinner={winner === 'player'}` to `HandNameOverlay`, which dims the hand name in the loss colour on a TIE, nine lines above a badge that says TIE in mint (:1012-1014). Display only; not the tally arithmetic. | reported |
| 14d | LOW | binary over three-way | `supabase/functions/resolve-hand/index.ts:85-101`: the losing claimant of the resolve race returns the pre-claim snapshot, whose `resolved` is `null` while the winner is still writing; the client (`utils/serverAdjudication.ts:122`) reads `ok && !outcome` as "refused: unknown" and ends the hand. Reachable only on a double invoke for one hand. | reported |
| 14e | info | present-and-empty | `get_daily_digest()` (migration 20260828120100:51-57) drops every pending session whose `raw_input` lacks an `AI Summary:` line — `SUBSTRING` → NULL → `||` → NULL → `string_agg` skips it — while `pending_sessions` still counts it. No cron, function, RPC or edge function calls `get_daily_digest` (`run_daily_digest` does not), and 0 sessions are pending today, so the cost is nil until someone wires it. | informational |
| 14f | settled | binary over three-way | `components/ShareCard.tsx:413,443` (full-game story) prints `boardsWon/allBoards.length` from a player-only count, so a tied board is in neither bucket; the compact card at :148 is tally-aware. Part of the settled tie-tally area — a new site, listed, not proposed for change. | noted |
| 15 | LOW-MEDIUM | name≠content | **Home tells veterans to "Play Sit and Go to see your history"** (`utils/i18n.ts:1253`, he :833) — Sit and Go has no route and no entry point; its tables were last written 2026-06-24. | reported — copy |
| 16 | LOW-MEDIUM | name≠content · hardcoded board count | **Cups subtitle hardcodes the board count and disagrees with itself across languages.** `utils/i18n.ts:669` Hebrew "ארבע קלפים. ארבעה בורדים." (four boards) vs :1090 English "4 cards. Every board." The board count is dynamic (2P=4, 3P=3, 4P=2). | reported — copy |
| 17 | LOW-MEDIUM | name≠content | **`submit_score`'s DB COMMENT describes the chip path removed on 2026-09-08** ("Echoes/raises a device leaderboard balance… +2,000 delta clamp"). A reader of `pg_description` believes the faucet is open. | reported — `COMMENT ON` is DDL |
| 18 | LOW-MEDIUM | name≠content | **Cron `daily_reconciliation` reconciles nothing about chips.** `run_daily_reconciliation` posts a `prompt_execution_log` count to Empire HQ with a literal `x-ingest-secret` in the function body. | reported |
| 19 | LOW-MEDIUM | landmine | **`fix-eas.js` writes another project's ASC key into caps.** It sets `eas.json` `build.production.ios.ascApiKeyPath/Id/IssuerId` to 9soccer's `6WF8UY7742`. Running it would point caps builds at the wrong Apple key. | reported — root file; recommend removal by Roye (never delete rule) |
| 20 | LOW (disabled) / HIGH if re-enabled | guard that cannot fail | **`claude-fix.yml:65` merges to main whatever Jest says.** `npx jest --forceExit 2>&1 \| tail -5; if [ $? -eq 0 ]` tests `tail`'s exit code. Workflow is `disabled_manually` since 2026-05-14. | reported |
| 21 | LOW (disabled) | guard that cannot fail | **`auto-fix-crashes.yml` can only ever get a 401.** It POSTs with no `Authorization` header (:14-16) to a function that requires `Bearer` (`supabase/functions/auto-fix-crashes/index.ts:24-25`, `verify_jwt: true`), then prints the code and only warns (:19-23). While it ran (1,106 runs to 2026-05-14) every run was green and nothing executed; `docs/CAPS_POKER_SKILL.md` and `MASTER_INDEX.md` still list it as running. | reported |
| 22 | LOW-MEDIUM | stale brief | **Empire HQ `bot_landing_brief('caps-poker')` — the first step of CLAUDE.md's quick start — lists "no Hebrew" and "no account deletion" as risks.** Both closed. | CLAUDE.md warned; the brief lives in project vjxqlqtlywovnbidovit |
| 23 | LOW | name≠content | `supabase/functions/analyze-bug-report/index.ts:8` `CURRENT_OTA = 'd36473c0'` is a constant, so every triaged report can carry an "OLD OTA" warning against a fixed value. | reported |
| 24 | LOW | comment≠code | `constants/gameConfig.ts:131-134` derives an overhead of 100 px; the code uses 120. `constants/visualThemes.ts:34` says "NOTHING READS THESE YET" for board colours that are routed. | reported |
| 25 | LOW | two sources of truth | "Have ever played" has three answers: `games_played > 0` = 25, `hands_played > 0` = 26, distinct `hand_history.device_id` = 10. 23 of the 25 have no `hand_history` row. | named in CLAUDE.md |
| 26 | info | — | `vamos_handoffs` max id 209, 207 rows: ids 19 and 172 are missing. | — |
| 27 | info | known | `asc-details.yml` / `asc-list.yml` ghost workflows: registered from deleted `recovery/may4-clean`, inert, undeletable via API (handoff 203). | indexed in `WORKFLOWS.md` |
| 28 | info | — | Inert backups: `_econ_fn_backup` (rollback artifact, no reader), `_backup_starter_redemptions_20260816` (RLS on, 0 policies), `_tmp_commit_blobs`. Safe to archive off-DB per TOTAL-AUDIT-2026-09-02; untouched. | — |

Minor, listed once (from the class-1 finders, each re-read): `fix-theme.cjs` patches `app/index.tsx` and `components/TournamentLobby.tsx`, neither exists (ENOENT if run) · `.gitattributes:1` names `eas-build-pre-install.sh`, absent · `sentry.options.json` with no sentry dependency or import · `.env.example` documents `SUPABASE_URL`/`SUPABASE_ANON_KEY` while the app reads `extra.supabaseUrl` and `EXPO_PUBLIC_SUPABASE_URL` · `packages/debugger/src/screenRecorder.ts` and `DebugOverlay.tsx` are diverged copies of the live `utils/`/`components/` files, exported by `packages/debugger/index.ts` and imported by nothing · `supabase/functions/sync-bugs-to-drive/index.ts` only `console.log`s the body (deployed v22) · `app/lobby/index.tsx:149` `playSolo` is defined and never rendered, so `solo_fallback_tapped` can never fire · `constants/economyConfig.ts:3` "All flags default to OFF" above four `true`s · `assets/sounds/README.txt` lists four `.mp3` names for a directory of 13 `.wav`.

## Class hunt coverage

| class | finder | verified by | result |
|---|---|---|---|
| 1 name ≠ content (files) | done | orchestrator re-read every cited line | 8 filed (#5, #8, #12, #19, #28 + minor list), 4 rejected |
| 1 name ≠ content (code) | done | orchestrator | #6, #7, #8, #15, #16, #23, #24 |
| 2 two sources of truth | done | orchestrator | #3, #9, #25, plus `get_current_build` wrapping `get_live_build` (consistent, noted in CLAUDE.md) |
| 3 binary over three-way | done | orchestrator | #14c, #14d, #14f; 44 sort comparators and every three-way site in BoardReveal/results/replay/statsEngine/resolve-hand read and rejected |
| 4 present and empty | done | orchestrator + live DB | #14a (latent, measured 0 affected), #14b, #14e; every `bool_or`/`max`/`sum` in the newest 25 migrations is coalesced |
| 5 guard that cannot fail | done | orchestrator | #4a, #10a, #10c, #10g, #20, #21; ~90 empty `catch {}` blocks read — all around haptics/sounds/router, none converts an error into reported success on an economy or settlement path |
| 6 dead-looking load-bearing / flags | see the flag table below | orchestrator | #14 |
| 7 number printed, never asserted | done | orchestrator | #4b, #10, #10b, #10d, #10e, #10f |

The three-lens adversarial verification the workflow was built for did not run — the session usage limit killed all 60 verifier agents — so every finding above was instead re-read by the orchestrator at the cited line and, where a number was involved, re-measured on the live database. Agent findings that did not survive that re-read are not listed.

## The flag table — every `app_config` key the brief named, measured three ways (client code, edge functions, live `pg_proc` bodies)

| key | value | client | edge fn | live SQL body | governs |
|---|---|---|---|---|---|
| `mp_board_reveal_enabled` | true | `app/_layout.tsx:552` → `multiplayer-game.tsx` | — | — | the MP reveal |
| `mp_server_adjudication_enabled` | true | `utils/serverAdjudication.ts:18` | — | — | MP settlement path |
| `phase0_channel_authz_enforced` | true | `utils/privateChannel.ts:78` | — | — | private Realtime channels |
| `pot_per_board` | 25 | — | `resolve-hand:38-42` | `claim_emergency_chips` | MP pot; emergency amount |
| `econ_binding_enabled` / `econ_throttle_enabled` | true | — | — | `econ_bind_ok` / `econ_rate_ok` | the identity and rate guards |
| `join_requires_session` | true | — | — | `join_table`, two tripwires | joining a table |
| `hand_rake_pct` | 5 | — | — | `record_hand_net` | the rake |
| `starting_chips` | 2000 | — | — | `join_sit_n_go*`, `caps_testflight_audit` | SNG only — the 2,000 grant itself is a column DEFAULT |
| `flash_deal_enabled` | true | — | — | `get_chip_store`, `send_flash_deal_push` | shop + a push |
| `level_system_enabled` | true | — | — | `get_level_config`, audit | levels |
| `quick_poker_enabled` | true | — | — | `get_home_screen_v2`, audit | home payload only |
| `pro_voices_enabled`, `streak_bonus_enabled`, `whatsapp_share_enabled` | true | — | — | `get_home_screen` | home payload |
| `starter_pack_2x_enabled` | true | — | — | `get_starter_offer_for_device` | starter offer |
| `winback_push_enabled` | true | — | — | `check_winback` | a push |
| `iap_enabled` | false | `utils/iapEnabled.ts:26` — loader never called (#14) | — | — | **nothing** |
| `web_payments_enabled` | false | `utils/webPayments.ts:38` — module has no importer | — | — | **nothing** |
| `all_time_leaderboard_enabled`, `battle_pass_enabled`, `company_league_enabled`, `d3_bonus_enabled`, `daily_reward_chips`, `dark_mode_enabled`, `emergency_chips_enabled`, `gifting_enabled`, `hand_rankings_overlay`, `hooks_crash_known`, `maintenance_mode`, `min_app_version`, `practice_mode_enabled`, `rewarded_ad_enabled`, `share_hands_enabled`, `show_tutorial_button`, `sit_n_go_enabled`, `starter_pack_enabled`, `tournament_waitlist_enabled`, `tutorial_enabled`, `wc_poker_event_enabled` | various | — | — | — | **nothing** (21 keys) |
| `run_marathon`, `premium_theme_enabled` | *(row absent)* | `_layout.tsx:569` / `PaintProvider.tsx:61` | — | — | read by the client, never written |

Client-side constants that are not remote: `PRACTICE_LIVE_ENABLED=false` (`constants/featureFlags.ts:28`, kills the practice-to-live overlay while `practiceLiveSession.test.ts` mocks it true), `ECONOMY_FLAGS` (2 of 6 read), `__DEV__`. `EXPO_PUBLIC_REVENUECAT_IOS_KEY/ANDROID_KEY` (`app/_layout.tsx:504-505`) are set nowhere in the repo or its workflows — whether an EAS-dashboard secret supplies them is UNMEASURABLE from here.


## The SQL for #1 and #2 (not applied — DDL on the economy's security surface is Roye's; ship as a migration file)

```sql
-- #1 the archive: same posture as the live ledger
ALTER TABLE public.chip_transactions_prereset_20260901 ENABLE ROW LEVEL SECURITY;
REVOKE ALL ON public.chip_transactions_prereset_20260901 FROM anon, authenticated, PUBLIC;
GRANT SELECT ON public.chip_transactions_prereset_20260901 TO service_role;

-- #2 the views: finish what 20260831170000 started for v_harness_devices
REVOKE SELECT ON public.v_harness_devices_v2, public.v_simulator_devices, public.v_grant_without_session,
                 public.friction_heatmap, public.top_stuck_screens, public.top_abandon_screens, public.top_rage_tap_targets
  FROM anon, authenticated, PUBLIC;
GRANT SELECT ON public.v_harness_devices_v2, public.v_simulator_devices, public.v_grant_without_session,
                public.friction_heatmap, public.top_stuck_screens, public.top_abandon_screens, public.top_rage_tap_targets
  TO service_role;
-- proof, before and after:  SET ROLE anon; SELECT count(*) FROM public.v_harness_devices_v2;  -- 8 now, must become "permission denied"
```

No repo file reads any of these views (grep, 2026-09-10); the only mention is MEMORY.md. A dashboard outside the repo could — check `pg_stat_statements` for their names before applying.

## What was NOT found (searched, not assumed)

- **One claim of my own withdrawn.** The truth sheet handed to the doc agents said the game is "not Omaha". That was an over-statement from reading `IRON_RULES.md`'s project block as stale: per-board evaluation IS Omaha-style (`utils/handEvaluator.ts` `evaluateOmahaHand`, used by `utils/gameLogic.ts:114` and `resolve-hand`), and "under Omaha rules" in the 2026-09-08 listing draft is a wording choice, not an error. Every "Omaha is wrong" line the agents produced was discarded and the three banners it leaked into were corrected before commit. What remains true: `IRON_RULES.md`'s project block is stale on the theme ("Central Perk") and on "no backend".
- No live cron failure: all 31 jobs succeeded every run in the last 7 days; the `up.is_bot does not exist` and `push_log.user_id` failures on the four push jobs are from 2026-04-12/13 and have not recurred.
- No stale bundle on the web: `web-deploy.yml` ran on `ef55640` at 2026-09-09 10:47 and the live site serves `index-cf4906c09ea1adde3053d25b9b93326b.js`; `/nope.png` and `/definitely-missing.html` return honest 404s.
- No `caps-explainer-FINAL.mp4` vendored under any name; the shipped clip is pinned by sha in `public/landing.html`.
- The four assets pairs that look alike and are identical are legitimate (`icon`=`adaptive-icon`, `splash`=`splash-icon`, `game-boards`=`game-boards-en`, `game-reveal`=`game-reveal-en`); the two cover pairs that look alike and differ are in `ASSETS.md`.
