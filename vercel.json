{
  "installCommand": "echo \"=================================================================================\" && echo \"THIS FILE IS NOT WHAT SERVES caps.ftable.co.il. PRODUCTION NEVER READS IT.\" && echo \"The site is deployed by .github/workflows/web-deploy.yml, which runs vercel --prod\" && echo \"from dist/ using the dist/vercel.json that scripts/fix-web-html.js WRITES. Any\" && echo \"rewrite, header or redirect that must ship goes in THAT GENERATOR. A catch-all 404\" && echo \"fix was put here on 2026-09-03, declared done, and was never live: four days later\" && echo \"/nope.png still returned 200 with the app HTML.\" && echo \"NEVER ADD A COMMENT KEY HERE. JSON has no comments, Vercel validates this file\" && echo \"against a CLOSED schema BEFORE the Ignored Build Step runs, and one _comment key\" && echo \"failed 29 of 40 deploys over five days in September 2026. Prose goes in the .js.\" && echo \"This file exists only so a Git-integration deploy has a correct config if the\" && echo \"project Ignored Build Step is ever turned off. Keep it identical to the generator.\" && echo \"=================================================================================\" && npm install",
  "buildCommand": "npx expo export --platform web --output-dir web-dist --clear && node scripts/fix-web-html.js",
  "outputDirectory": "web-dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/privacy.html",
      "destination": "/privacy.html"
    },
    {
      "source": "/terms.html",
      "destination": "/terms.html"
    },
    {
      "source": "/landing.html",
      "destination": "/landing.html"
    },
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Content-Security-Policy",
          "value": "frame-ancestors 'none'"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        }
      ]
    }
  ]
}
